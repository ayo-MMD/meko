/// State management / business logic providers for Meko.
///
/// This layer sits between the UI and the service layer.
/// Providers (or equivalent state holders) orchestrate data flow,
/// validate business rules, and expose reactive state to the UI.
library;
